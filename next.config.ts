import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // buildId is set via BUILD_ID environment variable
  // Run with: BUILD_ID=chatgpt npm run dev
};

export default nextConfig;
